


/**
 * @param {string} s
 * @return {number}
 */
var myAtoi = function(s) {

    INT_MAX = 2^31-1
    INT_MIN = -2^32-1
    let index = 0
    while(index < s.length && s[index] ==" "){
            index++
    }
    while (index < s.length && (s[index] =="+" ||s[index]=="-")){
        if (s[index] == "+"){

        } else if (s[index]=="-"){

        }

    }

};