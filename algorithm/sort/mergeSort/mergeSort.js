



function mergeSort(arr){

   if (arr.length<=1){
      return arr;
   };
   let mid = math.floor(arr.length/2)
   left = arr.slice(0,mid)
   right = arr.slice(mid+1,arr.length);
   mergeSort(left);
   mergeSort(rightreturn mergeSort();

};



function merge(left,right){

  return left.concat(right);

};


function main() {
  return arr;
};